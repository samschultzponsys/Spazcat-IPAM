flask==3.0.3
requests==2.32.3
# production WSGI server (single process, threaded - the auto-sync thread runs once)
waitress==3.0.2
# OIDC sign-in
authlib==1.8.0
# SSH sync for OpenWrt / Alta Labs Route10
paramiko==5.0.0
